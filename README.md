# before run tests u should do "npm i" 
# after "npm i" u need to type in cmd "npx cypress run" to run tests
# if you don't want to run some of tests just type "x" before "it" should be like this "xit"